# Domain Layer Repository Interfaces
This package defines abstract repository contracts for the domain layer.
These repositories act as boundaries between business logic (usecases) and the underlying data sources.